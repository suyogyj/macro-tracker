/**
 * Copy to config.js and fill in your Supabase project values.
 * config.js is gitignored — do not commit real keys in a public repo
 * (the anon key is safe to expose in frontend apps; security is enforced by RLS).
 */
window.MACRO_TRACKER_CONFIG = {
    supabaseUrl: 'https://YOUR_PROJECT_REF.supabase.co',
    supabaseAnonKey: 'YOUR_SUPABASE_ANON_KEY'
};
